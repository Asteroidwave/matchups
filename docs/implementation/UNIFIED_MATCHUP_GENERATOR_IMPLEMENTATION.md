# Unified Matchup Generator Implementation ## Overview I've implemented the same calculation logic as `unified_matchup_generator.py` in TypeScript. The backend now uses the **exact same algorithm** as your Python code. ## Key Features ### 1. **Hybrid Approach** - **Exhaustive search for 1v1 matchups**: Checks all pairs (like Python) - **Optimized random sampling for complex patterns**: Uses random sampling for 2v1, 1v2, etc. (like Python) ### 2. **Default Settings (Matching Python Code)** | Matchup Type | Count | Tolerance | Min Appearances | Patterns | |-------------|------|-----------|----------------|----------| | Jockey vs Jockey | 5 | 300 | 3 | (1,1), (2,1), (1,2) | | Trainer vs Trainer | 8 | 300 | 3 | (1,1), (2,1), (1,2) | | Sire vs Sire | 10 | 300 | 3 | (1,1), (2,1), (1,2) | | Mixed | 60 | 300 | 3 | (1,1), (2,1), (1,2) | ### 3. **Horse Overlap Checking** - Prevents matchups where connections share the same horses - Uses the same logic as Python: builds horse sets per connection and checks for intersections ### 4. **Tie Breaking** - If points are equal (or both 0), adds 0.01 to the side with higher salary - Ensures no ties in matchups ## Implementation Details ### Files Created/Modified 1. **`backend/src/services/unifiedMatchupGenerator.ts`** (NEW) - Contains the unified matchup generation logic - `generateUnifiedMatchups()`: Main function matching Python's `generate_matchups()` - `buildConnectionHorseMap()`: Builds horse-to-connection mapping - `checkHorseOverlap()`: Checks if two connection sets share horses 2. **`backend/src/services/matchupCalculation.ts`** (MODIFIED) - Now uses `generateUnifiedMatchups()` instead of simplified version - Uses `DEFAULT_UNIFIED_SETTINGS` from unified generator - Supports per-matchup-type settings 3. **`components/admin/MatchupCalculationSettings.tsx`** (MODIFIED) - Added `DEFAULT_SETTINGS_BY_TYPE` with per-type defaults - Updated general defaults to match unified generator (tolerance: 300) 4. **`components/admin/PerMatchupTypeSettings.tsx`** (MODIFIED) - Now uses per-type defaults when initializing settings - JvJ defaults to 5 matchups, TvT to 8, SvS to 10, Mixed to 60 ## How It Works ### TypeScript Can Handle These Calculations **Yes, TypeScript/Node.js can handle these calculations.** The algorithms are: - **CPU-bound**: Sorting, filtering, set operations - **Memory-efficient**: Uses Sets and Maps for O(1) lookups - **Optimized**: Limits random trials (like Python: min(20, len/3) for Set A, min(50, len*2) for Set B) ### Performance Comparison | Operation | Python (unified_matchup_generator.py) | TypeScript (backend) | |-----------|--------------------------------------|---------------------| | Exhaustive 1v1 | O(n²) - checks all pairs | O(n²) - same | | Random Sampling | Up to 500 attempts per matchup | Up to 500 attempts per matchup | | Horse Overlap | Set intersection | Set intersection (native JS) | | Memory Usage | Pandas DataFrames | Native JS objects/arrays | **TypeScript should perform similarly or faster** for these operations because: - Native JavaScript Set operations are highly optimized - No DataFrame overhead - V8 engine optimizations for arrays/objects ## Default Settings in UI When you open the settings dialog for each matchup type: - **Jockey vs Jockey**: Pre-filled with 5 matchups, 300 tolerance - **Trainer vs Trainer**: Pre-filled with 8 matchups, 300 tolerance - **Sire vs Sire**: Pre-filled with 10 matchups, 300 tolerance - **Mixed**: Pre-filled with 60 matchups, 300 tolerance You can modify these before clicking "Calculate Matchups". ## Timeout Fix Fixed the "Request timeout after 30 seconds" issue by: 1. **Simplified query**: Removed double `orderBy` (was ordering by date then track) 2. **Better error handling**: Properly handles Supabase query errors 3. **Query timing**: Logs query time to help diagnose slow queries The contest should now load properly in the admin panel. ## Testing 1. **Create a contest** with track data 2. **Set matchup types** - you'll see per-type defaults 3. **Modify settings** (optional) - or use defaults 4. **Calculate matchups** - uses unified generator logic 5. **Check results** - matchups will use exhaustive 1v1 + optimized complex patterns ## Next Steps The implementation is complete. The backend will: - Use exhaustive search for 1v1 matchups (like Python) - Use optimized random sampling for complex patterns (like Python) - Check horse overlap (like Python) - Use minimum appearances threshold of 3 (like Python) - Use default settings matching your Python code Try calculating matchups now - it should work with the same logic as your unified generator! 