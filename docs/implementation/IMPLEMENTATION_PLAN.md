# Implementation Plan - New Contest Flow ## Immediate Fixes (Do First) ### 1. Fix Admin Session Persistence **Problem:** Admin privileges lost on reload **Solution:** - Cache profile in localStorage with expiration - Refresh profile on page load before showing UI - Show loading state until profile is loaded ### 2. Fix Lobby Loading **Problem:** Lobby sometimes shows nothing **Solution:** - Add retry logic for failed API calls - Show loading states clearly - Cache contests in AppContext with refresh mechanism - Add error boundaries ### 3. Add Comprehensive Logging **Problem:** No visibility into what's happening **Solution:** - Structured logging with operation IDs - Frontend: Console logs with emojis for easy scanning - Backend: Operation tracking with timestamps - Network: Log all API calls with request/response ## Your Questions Answered ### Q1: Matchup calculation - auto or manual? **Recommendation:** Auto when matchup types are selected, but with manual "Recalculate" option - Admin selects types Backend starts calculation automatically - Show progress banner - Admin can manually trigger recalculation if needed ### Q2: Data storage - full data or metadata? **Recommendation:** Hybrid approach - **Supabase:** Store metadata (race count, post times, horse count) + reference to full data - **Redis:** Store full MongoDB data (fast access, 24h TTL) - **MongoDB:** Source of truth (always fetchable) ### Q3: Multiple matchup types per contest? **Yes!** A contest can have: - JvJ only - TvT only - JvJ + TvT (both types) - Mixed (all types) - Custom selection (any combination) ### Q4: Preview detail level? **Recommendation:** Show: - Total races - Post times (first race, last race) - Total horses - Track name - Date - Option to expand for full details ### Q5: Cache invalidation? - **Track data:** 24 hours (data doesn't change often) - **Matchups:** Until contest date passes - **Contests:** 1 hour (admin changes are infrequent) ## Architecture Overview ``` ┌─────────────────────────────────────────────────────────┐ │ Admin Creates Contest │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 1. Enter Track Code + Date │ │ └─> POST /api/admin/tracks/fetch │ │ └─> Fetch from MongoDB │ │ └─> Cache in Redis (24h) │ │ └─> Store metadata in Supabase track_data │ │ └─> Return preview │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 2. Show Preview │ │ - Races count │ │ - Post times │ │ - Horses count │ │ - [Confirm] button │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 3. Admin Confirms Create Contest │ │ └─> POST /api/admin/contests │ │ └─> Status: 'draft' │ │ └─> Appears in "Manage Contests" │ │ └─> NOT visible in lobby yet │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 4. Admin Selects Matchup Types │ │ └─> PUT /api/admin/contests/:id/matchup-types │ │ └─> Save types to contest │ │ └─> Trigger async calculation │ │ └─> Status: 'processing' │ │ └─> Show progress banner │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 5. Backend Calculates Matchups (Async) │ │ └─> Background job │ │ └─> Fetch track data from Redis │ │ └─> Generate matchups for each type │ │ └─> Store in Supabase matchups table │ │ └─> Update contest status: 'ready' │ │ └─> Send notification (banner) │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 6. Matchups Ready │ │ └─> Banner: "Matchups ready for GP on 2025-11-02" │ │ └─> Contest status: 'ready' │ │ └─> Visibility toggle now enabled │ └────────────────────┬────────────────────────────────────┘ │ ▼ ┌─────────────────────────────────────────────────────────┐ │ 7. Admin Toggles Visibility │ │ └─> PUT /api/admin/contests/:id/visibility │ │ └─> is_active = true/false │ │ └─> Contest appears in lobby │ └─────────────────────────────────────────────────────────┘ ``` ## Data Flow ### Track Data Flow ``` MongoDB (Source) Backend API (Fetch) Redis Cache (24h TTL) Fast access Supabase track_data (Metadata) Permanent storage Frontend (Preview) ``` ### Contest Flow ``` Admin Form Backend API Supabase contests (status: 'draft') Admin selects matchup types Backend calculates (async) Supabase matchups (Full data) Supabase contests (status: 'ready') Admin toggles visibility Lobby (visible to players) ``` ## Implementation Steps ### Step 1: Fix Immediate Issues (1-2 hours) - [ ] Fix admin session persistence - [ ] Fix lobby loading consistency - [ ] Add error boundaries - [ ] Add basic logging ### Step 2: Database Schema (1 hour) - [ ] Create `track_data` table - [ ] Create `matchups` table - [ ] Create `operations` table - [ ] Update `contests` table - [ ] Add RLS policies ### Step 3: Backend APIs (4-6 hours) - [ ] Track data fetch endpoint - [ ] Track data preview endpoint - [ ] Enhanced contest creation - [ ] Matchup calculation endpoint (async) - [ ] Operation tracking - [ ] Status endpoints ### Step 4: Frontend Components (4-6 hours) - [ ] FetchTrackDataForm - [ ] TrackDataPreview - [ ] ContestConfiguration - [ ] OperationStatusBanner - [ ] Enhanced ContestManagement - [ ] Loading states & error handling ### Step 5: Testing & Polish (2-3 hours) - [ ] Test all flows - [ ] Test interruptions - [ ] Test error scenarios - [ ] Performance optimization - [ ] Documentation **Total Estimated Time:** 12-18 hours ## Next Steps 1. **Confirm:** Do you want me to start with immediate fixes? 2. **Clarify:** Any changes to the flow I proposed? 3. **Priority:** Should I implement everything at once, or phase it? Let me know and I'll start implementing! 