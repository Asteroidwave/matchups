# Quick Start Checklist ## Pre-Flight Check ### 1. Verify Backend `.env` Has Supabase Check `backend/.env` contains: ```bash SUPABASE_URL=https://xdvtdifqcukahmzcbent.supabase.co SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here ``` **To get service_role key:** - Supabase Dashboard Settings API - Find **"service_role"** key (not anon) - Copy and paste into `backend/.env` --- ## Restart Steps ### Step 1: Kill Everything ```bash # Kill frontend (Ctrl+C) # Kill backend (Ctrl+C) ``` ### Step 2: Delete Supabase Tables - Supabase Dashboard SQL Editor - Run: `DROP TABLE IF EXISTS contests CASCADE; DROP TABLE IF EXISTS profiles CASCADE;` ### Step 3: Run Fresh SQL - Copy entire `docs/FRESH_SUPABASE_SETUP.sql` - Paste in Supabase SQL Editor - Click "Run" ### Step 4: Make Yourself Admin - Get your User ID: Authentication Users Copy UUID - Run in SQL Editor: ```sql INSERT INTO profiles (id, email, is_admin, bankroll) VALUES ('YOUR-USER-ID', 'ritho@ralls.com', TRUE, 1000.00) ON CONFLICT (id) DO UPDATE SET is_admin = TRUE; ``` ### Step 5: Start Backend ```bash cd backend npm run dev ``` Look for: ` Supabase backend client initialized` ### Step 6: Start Frontend ```bash # In project root npm run dev ``` ### Step 7: Test 1. Login Should see admin badge 2. Go to `/admin` Should work 3. Create contest Should work 4. Users tab Should load --- ## Full Testing Guide See `docs/COMPLETE_RESTART_GUIDE.md` for detailed testing steps. 